# 🚀 Запуск Streamlit приложения

## 📋 Оглавление

1. [Требования](#требования)
2. [Установка зависимостей](#установка-зависимостей)
3. [Подготовка данных](#подготовка-данных)
4. [Запуск приложения](#запуск-приложения)
5. [Использование](#использование)
6. [Troubleshooting](#troubleshooting)

---

## 1. Требования

### Минимальные требования:

- **Python**: 3.8 или выше
- **Память**: 8 GB RAM минимум (16 GB рекомендуется)
- **GPU**: Опционально (CUDA-совместимая для ускорения)
- **Место на диске**: ~500 MB для зависимостей

### Уже установлено (из requirements.txt):

- ✅ PyTorch
- ✅ Segmentation Models PyTorch
- ✅ OpenCV
- ✅ Numpy, Pillow, etc.

### Дополнительные зависимости для Streamlit:

```
streamlit>=1.28.0
```

---

## 2. Установка зависимостей

### Шаг 1: Активируйте виртуальное окружение

**Windows:**
```bash
cd F:\inria_project
.\venv\Scripts\activate
```

**Linux/Mac:**
```bash
cd /path/to/inria_project
source venv/bin/activate
```

### Шаг 2: Установите Streamlit

```bash
pip install streamlit
```

### Шаг 3: Проверка установки

```bash
streamlit --version
```

Должно показать что-то вроде: `Streamlit, version 1.28.0`

---

## 3. Подготовка данных

### 3.1 Убедитесь, что модель обучена

Проверьте наличие файла:
```
models/checkpoints/best_model.pth
```

Если файла нет - запустите обучение:
```bash
python train_script.py --config configs/config.yaml
```

### 3.2 Проверьте конфигурацию

Убедитесь, что файл существует:
```
configs/config.yaml
```

### 3.3 (Опционально) Добавьте примеры изображений

Скопируйте несколько изображений в папку `examples/`:

```bash
# Создайте папку если её нет
mkdir examples

# Скопируйте примеры из датасета
cp data/AerialImageDataset/train/images/austin1.tif examples/
cp data/AerialImageDataset/train/images/vienna10.tif examples/
cp data/AerialImageDataset/train/images/chicago5.tif examples/
```

**Папка examples/** используется для демонстрации, но **не обязательна** - пользователь может загружать свои изображения.

---

## 4. Запуск приложения

### Основная команда:

```bash
streamlit run app.py
```

### После запуска:

1. **Терминал покажет:**
   ```
   You can now view your Streamlit app in your browser.
   
   Local URL: http://localhost:8501
   Network URL: http://192.168.1.X:8501
   ```

2. **Автоматически откроется браузер** по адресу `http://localhost:8501`

3. Если браузер не открылся - откройте вручную: `http://localhost:8501`

### Опции запуска:

**Указать порт:**
```bash
streamlit run app.py --server.port 8502
```

**Отключить автоматическое открытие браузера:**
```bash
streamlit run app.py --server.headless true
```

**Включить debug режим:**
```bash
streamlit run app.py --logger.level debug
```

---

## 5. Использование

### 5.1 Главный экран

При запуске вы увидите:

```
┌─────────────────────────────────────────────┐
│  🏙️ Building Segmentation from Satellite   │
│        Автоматическая сегментация           │
├─────────────────────────────────────────────┤
│  Sidebar (настройки)                        │
│  - Путь к модели                            │
│  - Параметры inference                      │
│  - Визуализация                             │
└─────────────────────────────────────────────┘
```

### 5.2 Боковая панель (Sidebar)

**⚙️ Настройки:**

1. **🤖 Модель:**
   - Путь к config.yaml
   - Путь к checkpoint (.pth)

2. **🔧 Параметры:**
   - **Threshold** (0.0 - 1.0): Порог бинаризации
   - **Patch size**: Размер патча для inference (256/512/1024)
   - **Stride**: Шаг скользящего окна

3. **🎨 Визуализация:**
   - **Colormap**: Цветовая схема (Red/Blue/Green/Jet/Viridis)
   - **Overlay alpha**: Прозрачность наложения

### 5.3 Три вкладки:

#### Tab 1: 📤 Загрузить изображение

1. **Нажмите** "Browse files"
2. **Выберите** спутниковый снимок (PNG, JPG, TIF)
3. **Нажмите** кнопку "🚀 Запустить сегментацию"
4. **Дождитесь** результата (~2-5 секунд)

**Результаты:**
- 🏗️ Площадь застройки (га, м²)
- 📍 Количество пикселей зданий
- 📈 Процент покрытия
- 🎨 4 вида визуализации
- 💾 Скачивание результатов

#### Tab 2: 🖼️ Примеры

1. **Выберите** пример из выпадающего списка
2. **Нажмите** "🚀 Запустить сегментацию"
3. **Получите** результаты

> ⚠️ Если примеры не добавлены - появится предупреждение

#### Tab 3: ℹ️ О проекте

- Описание проекта
- Архитектура модели
- Метрики качества
- Датасет
- Технологии

### 5.4 Визуализация результатов

После обработки изображения доступны 4 вкладки:

1. **🔴 Маска зданий** - бинарная маска в цвете
2. **🌈 Overlay** - маска наложенная на оригинал
3. **📊 Probability Map** - карта вероятностей + гистограмма
4. **📐 Сравнение** - три панели (Original | Mask | Overlay)

### 5.5 Скачивание результатов

Три кнопки:
- **📥 Скачать маску** (PNG)
- **📥 Скачать overlay** (PNG)
- **📥 Скачать данные** (JSON)

**Формат JSON:**
```json
{
  "area_m2": 658900.0,
  "area_ha": 65.89,
  "building_pixels": 7321111,
  "coverage_percent": 29.28,
  "image_size": [5000, 5000],
  "threshold": 0.5
}
```

---

## 6. Troubleshooting

### Проблема 1: "ModuleNotFoundError: No module named 'streamlit'"

**Решение:**
```bash
pip install streamlit
```

### Проблема 2: "FileNotFoundError: configs/config.yaml"

**Решение:**
Убедитесь, что вы в корневой папке проекта:
```bash
cd F:\inria_project
streamlit run app.py
```

### Проблема 3: "Checkpoint не найден"

**Решение:**
Проверьте путь к checkpoint в sidebar или укажите правильный:
```
models/checkpoints/best_model.pth
```

### Проблема 4: Медленная работа на CPU

**Решение:**
Уменьшите размеры:
- Patch size: 512 → 256
- Stride: 512 → 256

Или используйте GPU:
```bash
# Проверить доступность GPU
python -c "import torch; print(torch.cuda.is_available())"
```

### Проблема 5: "CUDA out of memory"

**Решение:**
Уменьшите размер патча:
- Patch size: 1024 → 512 → 256

### Проблема 6: Приложение не открывается в браузере

**Решение:**
Откройте вручную:
```
http://localhost:8501
```

Или попробуйте другой порт:
```bash
streamlit run app.py --server.port 8502
```

### Проблема 7: "Error loading model"

**Проверьте:**
1. ✅ Checkpoint существует
2. ✅ Config правильный
3. ✅ PyTorch установлен
4. ✅ GPU drivers (если используется)

**Попробуйте загрузить модель вручную:**
```bash
python -c "
import torch
checkpoint = torch.load('models/checkpoints/best_model.pth', map_location='cpu')
print('Model loaded successfully!')
"
```

---

## 7. Дополнительные команды

### Очистить кеш Streamlit:

```bash
streamlit cache clear
```

### Посмотреть конфигурацию Streamlit:

```bash
streamlit config show
```

### Остановить приложение:

Нажмите **Ctrl+C** в терминале

---

## 8. Структура приложения

```
inria_project/
├── app.py                      # Главное приложение Streamlit
├── src/
│   ├── app_utils.py           # Вспомогательные функции
│   ├── model.py               # Модель
│   ├── inference.py           # Inference
│   └── area_calculator.py     # Расчёт площади
├── configs/
│   └── config.yaml            # Конфигурация
├── models/
│   └── checkpoints/
│       └── best_model.pth     # Обученная модель
└── examples/                  # Примеры (опционально)
    ├── austin1.tif
    └── vienna10.tif
```

---

## 9. Запуск в production (опционально)

### Для deployment на сервере:

```bash
streamlit run app.py \
    --server.port 8501 \
    --server.address 0.0.0.0 \
    --server.headless true \
    --server.enableCORS false \
    --server.enableXsrfProtection false
```

### С использованием nohup (Linux):

```bash
nohup streamlit run app.py > streamlit.log 2>&1 &
```

---

## 10. Полезные ссылки

**Streamlit документация:**
- [Streamlit Docs](https://docs.streamlit.io/)
- [API Reference](https://docs.streamlit.io/library/api-reference)
- [Gallery](https://streamlit.io/gallery)

**Ваш проект:**
- GitHub: (добавьте ссылку)
- Документация: `FULL_PROJECT_GUIDE_RU.md`

---

## ✅ Чеклист перед запуском

- [ ] Виртуальное окружение активировано
- [ ] Streamlit установлен (`streamlit --version`)
- [ ] Модель обучена (`best_model.pth` существует)
- [ ] Config файл на месте (`config.yaml`)
- [ ] (Опционально) Примеры добавлены в `examples/`
- [ ] Запущена команда `streamlit run app.py`
- [ ] Браузер открылся на `http://localhost:8501`

---

## 🎉 Готово!

Приложение запущено и готово к использованию!

**Протестируйте:**
1. Загрузите тестовое изображение
2. Проверьте все вкладки визуализации
3. Скачайте результаты
4. Попробуйте изменить параметры в sidebar

**Для дипломной работы:**
- Сделайте скриншоты интерфейса
- Запишите демо-видео (Screen Recorder)
- Подготовьте презентацию с примерами

---

**Автор:** Zajinskiy Vadim
**Дата:** Январь 2026  
**Проект:** Building Segmentation - Streamlit App
