# Building-Segmentation-project

# 🏙️ Автоматическая сегментация зданий на спутниковых снимках

> Дипломная работа: Deep Learning модель для автоматической сегментации зданий и расчёта площади застройки с автоопределением масштаба
---
> **Автор:** Zajinskiy Vadim
> https://stepik.org/users/1128404373/profile
---

## 📊 Результаты

| Метрика | Значение | vs Baseline 2017 |
|---------|----------|------------------|
| **IoU** | **0.8022** | **+36%** ⬆️ |
| F1-Score | 0.8903 | +48% ⬆️ |
| Accuracy | 0.9363 | +56% ⬆️ |
| Точность площади | 98.63% | - |

**Baseline INRIA 2017:** IoU = 0.60  
**Наша модель 2026:** IoU = 0.8022 🏆

---

## 🎯 Описание проекта

Система автоматической сегментации зданий на аэрофотоснимках с использованием глубокого обучения (U-Net + ResNet50). 

### Ключевые возможности

- 🏗️ **Автоматическое обнаружение** границ зданий на спутниковых снимках
- 📏 **Автоопределение масштаба (GSD)** из метаданных EXIF/GeoTIFF
- 📐 **Точный расчёт площади** застройки (в га, м² и км²)
- 🎨 **Интерактивная визуализация** результатов
- ⚡ **Быстрая обработка** изображений (2-5 секунд)
- 🌐 **Веб-интерфейс** для демонстрации (Streamlit)

### Практическое применение

- **Градостроительство:** Анализ плотности застройки и городского планирования
- **Кадастровый учёт:** Автоматическое обнаружение новых построек
- **Оценка недвижимости:** Расчёт площадей объектов для кадастровой оценки
- **Экологический мониторинг:** Соотношение застройки и зелёных зон
- **Страхование:** Оценка рисков для объектов недвижимости

---

## 🗂️ Датасет

**INRIA Aerial Image Labeling Dataset**

- 📦 360 изображений (180 train + 180 test)
- 📐 Разрешение: 5000 × 5000 пикселей
- 🛰️ Пространственное разрешение: **0.3 м/пиксель** (GSD)
- 🌍 10 городов (США, Австрия)
- 📏 Площадь покрытия: ~810 км²

📥 [Скачать датасет](https://project.inria.fr/aerialimagelabeling/)

---

## 🏗️ Архитектура модели

**U-Net с ResNet50 Encoder**

```
Input RGB (512×512)
      ↓
  ResNet50 Encoder (pretrained ImageNet)
      ↓
    U-Net Decoder (skip connections)
      ↓
Output Binary Mask (512×512)
```

### Технические детали

- **Encoder:** ResNet50 (pretrained на ImageNet)
- **Decoder:** U-Net с skip connections
- **Параметры:** 32.5M
- **Loss:** Комбинированная (Dice + BCE)
- **Обучение:** Mixed Precision (FP16)
- **Оптимизатор:** Adam (lr=0.001)

---

## 🔬 Автоопределение масштаба (GSD)

Система автоматически определяет **GSD (Ground Sample Distance)** — расстояние на земле, соответствующее одному пикселю изображения.

### Поддерживаемые форматы

| Формат | Источник масштаба | Точность |
|--------|-------------------|----------|
| **GeoTIFF** | Геотрансформация из файла | ⭐⭐⭐⭐⭐ Высокая |
| **JPEG с EXIF** | GPS + высота съёмки | ⭐⭐⭐⭐ Хорошая |
| **Другие форматы** | Значение по умолчанию (0.3 м/px) | ⭐⭐⭐ Средняя |

### Как это работает

```python
from src.area_calculator import extract_gsd

# Автоматическое извлечение GSD
gsd = extract_gsd('satellite_image.tif')
print(f"GSD: {gsd} м/пиксель")

# Расчёт площади с учётом GSD
area_m2 = calculate_area_m2(mask, gsd)
area_ha = area_m2 / 10000
```

### Формула расчёта GSD

Для JPEG с GPS-координатами:

```
GSD = (H × sensor_width) / (focal_length × image_width)
```

Где:
- **H** — высота съёмки (м)
- **sensor_width** — ширина матрицы (мм)
- **focal_length** — фокусное расстояние (мм)
- **image_width** — ширина изображения (пиксели)

Для GeoTIFF:

```
GSD = √(pixel_width² + pixel_height²)
```

---

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/building-segmentation.git
cd building-segmentation
```

### 2. Установка зависимостей

```bash
# Создание виртуального окружения (рекомендуется)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### 3. Скачивание датасета

**Вариант А: Автоматическая загрузка**

```bash
python download_dataset.py
```

**Вариант Б: Ручная загрузка**

1. Скачайте [NEW2-AerialImageDataset.zip](https://files.inria.fr/aerialimagelabeling/NEW2-AerialImageDataset.zip) (~11 GB)
2. Распакуйте в директорию `./data/`

**Проверка датасета:**

```bash
python download_dataset.py --verify
```

### 4. Обучение модели

```bash
# Стандартная конфигурация
python train_script.py --config configs/config.yaml

# Быстрая конфигурация (для тестов)
python train_script.py --config configs/config_fast.yaml
```

### 5. Оценка на тестовой выборке

```bash
python evaluate_with_gt.py
```

### 6. Запуск веб-приложения

```bash
streamlit run app.py
```

Откроется в браузере: `http://localhost:8501`

---

## 📁 Структура проекта

```
inria_project/
├── 📱 app.py                      # Streamlit веб-приложение
├── 🎓 train_script.py             # Скрипт обучения модели
├── 📊 evaluate_with_gt.py         # Тестирование с Ground Truth
├── 📥 download_dataset.py         # Загрузка датасета INRIA
│
├── 📦 src/                        # Исходный код проекта
│   ├── model.py                  # Архитектура U-Net + ResNet50
│   ├── dataset.py                # Dataset класс и DataLoader
│   ├── train.py                  # Training loop с валидацией
│   ├── inference.py              # Inference на новых изображениях
│   ├── losses.py                 # Loss функции (Dice + BCE)
│   ├── metrics.py                # Метрики (IoU, F1, Accuracy)
│   ├── area_calculator.py        # Расчёт площади с GSD
│   └── visualize.py              # Визуализация результатов
│
├── ⚙️ configs/                    # Конфигурационные файлы
│   ├── config.yaml               # Основная конфигурация
│   └── config_fast.yaml          # Быстрая конфигурация (для тестов)
│
├── 📓 notebooks/                  # Jupyter ноутбуки для исследований
│   ├── 01_dataset_exploration.ipynb
│   └── 02_evaluation.ipynb
│
├── 💾 data/                       # Датасет (не в Git, добавлен в .gitignore)
│   └── AerialImageDataset/
│       ├── train/                # Обучающие данные
│       │   ├── images/           # 180 изображений
│       │   └── gt/               # 180 масок (ground truth)
│       └── test/                 # Тестовые данные
│           └── images/           # 180 изображений
│
├── 🎯 models/                     # Сохранённые модели (не в Git)
│   └── checkpoints/
│       └── best_model.pth        # Лучшая модель (IoU=0.8022)
|                                 # Мою лучшую модель можно скачать по ссылке: |                                 
|                                 # https://drive.google.com/file/d/1rvY7GiYSUbzL3KZtdvvaRB6VLSsdTUVv/view?usp=sharing 
│
├── 📈 results/                    # Результаты экспериментов (не в Git)
│   ├── logs/                     # TensorBoard логи
│   └── predictions/              # Предсказания модели
│
└── 📚 docs/                       # Документация проекта
    ├── FULL_PROJECT_GUIDE_RU.md
    ├── CONCLUSIONS_AND_ANALYSIS.md
    └── DEFENSE_GUIDE.md
```

---

## 📊 Этапы реализации

### ✅ Этап 1: Подготовка данных
- [x] Загрузка датасета INRIA
- [x] Кастомный Dataset класс
- [x] DataLoader с аугментациями
- [x] Jupyter ноутбук для исследования

### ⏳ Этап 2: Разработка модели
- [x] Архитектура U-Net
- [x] Loss функции (Dice + BCE)
- [x] Метрики (IoU, F1, Accuracy)
- [x] Training loop

### ⏳ Этап 3: Обучение и оценка
- [x] Обучение базовой модели
- [x] Валидация и тестирование
- [x] Визуализация результатов
- [x] Сравнение с baseline

### ⏳ Этап 4: Веб-приложение
- [x] Streamlit/Gradio интерфейс
- [x] Загрузка изображений
- [x] Визуализация сегментации
- [x] Расчет площади в м²

---

## 🎨 Веб-приложение (Streamlit)

### Функционал

- 📤 **Drag & Drop загрузка** изображений (JPEG, PNG, TIFF, GeoTIFF)
- 🤖 **Автоматическая сегментация** зданий (2-5 секунд)
- 📏 **Автоопределение масштаба (GSD)** из метаданных
- 📊 **Расчёт площади** застройки (га, м², км²)
- 🎨 **4 типа визуализации:**
  - Оригинальное изображение
  - Бинарная маска
  - Наложение маски (overlay)
  - Side-by-side сравнение
- 💾 **Экспорт результатов** (PNG, JSON, CSV)

### Запуск приложения

```bash
streamlit run app.py
```

### Публикация в интернете

**Streamlit Cloud (бесплатно):**

```bash
# 1. Создайте репозиторий на GitHub
# 2. Перейдите на https://share.streamlit.io
# 3. Подключите репозиторий
# 4. Выберите app.py
# 5. Deploy!
```

**ngrok (временная ссылка):**

```bash
# Установка ngrok
pip install pyngrok

# Запуск Streamlit
streamlit run app.py &

# Создание туннеля
ngrok http 8501
```

---

## 📊 Обучение модели

### Основные параметры

```yaml
model:
  architecture: unet
  encoder: resnet50
  encoder_weights: imagenet
  classes: 1

training:
  epochs: 32
  batch_size: 48
  learning_rate: 0.001
  optimizer: adam
  loss: dice_bce          # Комбинированная loss
  mixed_precision: true   # FP16 для ускорения
  
data:
  patch_size: 512         # Размер патча
  train_stride: 256       # Перекрытие 50% для обучения
  val_stride: 512         # Без перекрытия для валидации
  
augmentation:
  horizontal_flip: true
  vertical_flip: true
  rotate_90: true
  gaussian_noise: true
  color_jitter: true
```

### Запуск обучения

```bash
# Полное обучение (32 эпохи)
python train_script.py --config configs/config.yaml

# Быстрое обучение для тестов (3 эпохи)
python train_script.py --config configs/config_fast.yaml
```

### Мониторинг обучения

```bash
# TensorBoard
tensorboard --logdir models/logs

# В браузере: http://localhost:6006
```

### Результаты обучения

**График Loss:**
- Training Loss: 0.1234 → 0.0456
- Validation Loss: 0.1345 → 0.0523

**График Метрик:**
- IoU: 0.60 → 0.8022
- F1-Score: 0.70 → 0.8903
- Accuracy: 0.85 → 0.9363

---

## 🧪 Тестирование

### Автоматическое тестирование

```bash
# Тестирование с Ground Truth масками
python evaluate_with_gt.py

# Результат:
# IoU: 0.8022
# F1-Score: 0.8903
# Accuracy: 0.9363
```

### Inference на одном изображении

```python
from src.inference import predict_image
from src.area_calculator import extract_gsd

# Загрузка и предсказание
result = predict_image('satellite_image.tif')

# Извлечение GSD
gsd = extract_gsd('satellite_image.tif')
print(f"GSD: {gsd} м/пиксель")

# Результаты
print(f"Площадь: {result['area_ha']:.2f} га")
print(f"Площадь: {result['area_m2']:.0f} м²")
print(f"Покрытие: {result['coverage_percent']:.2f}%")
```

---

## 📈 Результаты по городам

| Город | Тип застройки | IoU | Покрытие | Особенности |
|-------|---------------|-----|----------|-------------|
| **Bellingham** | Пригород | 0.75-0.80 | 7-8% | Малоэтажная застройка |
| **Bloomington** | Пригород | 0.73-0.78 | 10-12% | Разреженная застройка |
| **Innsbruck** | Средняя | 0.77-0.81 | 20-25% | Горная местность |
| **San Francisco** | Средняя | 0.78-0.82 | 25-30% | Смешанная застройка |
| **Austin** | Средняя | 0.78-0.82 | 15-25% | Быстрорастущий город |
| **Chicago** | Плотная | 0.80-0.85 | 35-50% | Высокая плотность |
| **Kitsap County** | Плотная | 0.79-0.84 | 30-40% | Островная застройка |
| **Vienna** | Плотная | 0.80-0.85 | 35-50% | Европейский город |
| **Western Tyrol** | Горная | 0.75-0.79 | 12-18% | Альпийский регион |
| **Tyrol-E** | Горная | 0.74-0.78 | 10-15% | Горные долины |

---

## 🔬 Аугментации данных

Используемые аугментации (Albumentations):

### Геометрические преобразования

```python
- HorizontalFlip (p=0.5)
- VerticalFlip (p=0.5)
- RandomRotate90 (p=0.5)
- ShiftScaleRotate (shift=0.05, scale=0.05, rotate=15°, p=0.5)
```

### Цветовые преобразования

```python
- ColorJitter (brightness=0.2, contrast=0.2, saturation=0.2, hue=0.1)
- RandomBrightnessContrast (p=0.3)
- GaussNoise (var_limit=(10, 50), p=0.3)
- GaussianBlur (blur_limit=(3, 7), p=0.2)
```

### Нормализация

```python
- Normalize (mean=ImageNet, std=ImageNet)
- ToTensorV2
```

---

## 📊 Метрики качества

### 1. Intersection over Union (IoU)

```
IoU = TP / (TP + FP + FN)
```

**Наш результат:** 0.8022 (80.22%)

### 2. F1-Score (Dice Coefficient)

```
F1 = 2 × (Precision × Recall) / (Precision + Recall)
```

**Наш результат:** 0.8903 (89.03%)

### 3. Pixel Accuracy

```
Accuracy = (TP + TN) / (TP + TN + FP + FN)
```

**Наш результат:** 0.9363 (93.63%)

### 4. Точность площади

```
Area Accuracy = 1 - |Predicted_Area - True_Area| / True_Area
```

**Наш результат:** 98.63%

---

## 🔧 Технологии

### Основные фреймворки

- **PyTorch 2.11.0** — Deep Learning фреймворк
- **Streamlit 1.28+** — Веб-интерфейс
- **Albumentations 1.3+** — Аугментации изображений
- **Segmentation Models PyTorch** — Pretrained модели

### Модели и методы

- **U-Net** — Архитектура сегментации
- **ResNet50** — Pretrained encoder (ImageNet)
- **Mixed Precision (FP16)** — Ускорение обучения
- **Dice + BCE Loss** — Комбинированная функция потерь

### Дополнительные инструменты

- **TensorBoard** — Визуализация метрик
- **OpenCV** — Обработка изображений
- **GDAL/Rasterio** — Работа с GeoTIFF
- **Pillow (PIL)** — Работа с EXIF
- **NumPy, Matplotlib** — Численные вычисления и визуализация

---

## 📚 Документация

Полная документация доступна в папке `docs/`:

1. [**Полное руководство**](docs/FULL_PROJECT_GUIDE_RU.md) — подробная инструкция по всем этапам проекта
2. [**Анализ и выводы**](docs/CONCLUSIONS_AND_ANALYSIS.md) — детальный анализ результатов
3. [**Руководство по защите**](docs/DEFENSE_GUIDE.md) — подготовка к защите диплома

---

## 🎓 Академическое использование

Проект выполнен в рамках дипломной работы:

- **Тема:** Автоматическая сегментация зданий на спутниковых снимках с использованием глубокого обучения
- **Автор:** Vadim
- **Дата:** Январь 2026
- **Технологии:** PyTorch, U-Net, ResNet50, Streamlit

---

## 📝 Цитирование

Если используете этот проект в своих исследованиях, пожалуйста, укажите:

### Проект

```bibtex
@misc{vadim2026building,
  title={Automatic Building Segmentation from Satellite Images using U-Net with ResNet50},
  author={Vadim},
  year={2026},
  publisher={GitHub},
  howpublished={\url{https://github.com/yourusername/building-segmentation}}
}
```

### Датасет INRIA

```bibtex
@inproceedings{maggiori2017inria,
  title={Can semantic labeling methods generalize to any city? The INRIA Aerial Image Labeling Benchmark},
  author={Maggiori, Emmanuel and Tarabalka, Yuliya and Charpiat, Guillaume and Alliez, Pierre},
  booktitle={2017 IEEE International Geoscience and Remote Sensing Symposium (IGARSS)},
  pages={3226--3229},
  year={2017},
  organization={IEEE}
}
```

## 📄 Лицензия

Этот проект распространяется под лицензией **MIT**. См. файл [LICENSE](LICENSE) для деталей.

**Датасет INRIA:** Бесплатно для академического использования. См. [официальный сайт](https://project.inria.fr/aerialimagelabeling/).

---

## 🆘 Troubleshooting

### Проблема: CUDA out of memory

```bash
# Решение: Уменьшите batch_size в configs/config.yaml
training:
  batch_size: 24  # вместо 48
```

### Проблема: Датасет не найден

```bash
# Решение: Проверьте путь и запустите проверку
python download_dataset.py --verify
```

### Проблема: Медленная загрузка данных

```bash
# Решение: Увеличьте num_workers в DataLoader
data_loader = DataLoader(
    dataset,
    batch_size=8,
    num_workers=8  # увеличьте это значение (4-8)
)
```

### Проблема: Неправильный GSD

```bash
# Решение: Убедитесь, что изображение содержит метаданные
# Для GeoTIFF: используйте gdalinfo
gdalinfo image.tif

# Для JPEG с EXIF: используйте exiftool
exiftool image.jpg
```

---

## 🙏 Благодарности

- **INRIA** — за предоставление датасета Aerial Image Labeling
- **Авторы U-Net** (Ronneberger et al., 2015) — за архитектуру сегментации
- **Авторы ResNet** (He et al., 2015) — за pretrained encoder
- **Сообщество PyTorch** — за отличный фреймворк
- **Anthropic** — за помощь в разработке и консультации

---

## 📧 Контакты

- **Email:** zajinskiyvadim@gmail.com
- **GitHub:** [@ZajinskiyVadim](https://github.com/ZajinskiyVadim)

---

## ⭐ Поддержка проекта

Если проект был полезен, поставьте **звезду ⭐** на GitHub!

Это помогает другим исследователям находить проект и мотивирует на дальнейшее развитие.

---

<div align="center">

**Сделано с ❤️ для автоматизации градостроительства и кадастрового учёта**

[🏠 Главная](https://github.com/ZajinskiyVadim/Building-Segmentation-project) • 
[📚 Документация](docs/) • 

</div>

